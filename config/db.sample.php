<?php defined('ROOT_PATH') or exit('Direct access denied');
/**
 * Database credentials — SAMPLE.
 * The web installer (/install/) generates the real /config/db.php with these constants.
 * The guard above prevents direct browser access; includes from config.php still work
 * because config.php defines ROOT_PATH first.
 */
define('DB_HOST', 'localhost');
define('DB_NAME', 'ak_menu');
define('DB_USER', 'root');
define('DB_PASS', '');
define('DB_PREFIX', 'ak_');
define('DB_CHARSET', 'utf8mb4');
